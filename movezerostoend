#include<iostream>
#include<algorithm>
#include<vector>
using namespace std;
int main(){
vector<int> v1(5);
for(int i=0;i<v1.size();i++){
cout<<"v1["<<i<<"] ";
cin>>v[i];
}
sort(v1.begin(),v1.end());
for(int x:v1){
if(x==0){
rotate(v1.begin(),v1.begin()+1,v1.end());
}
}
for(int x:v1){
cout<<x<<" ";
}
return 0;
}
