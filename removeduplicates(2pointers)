#include<iostream>
#include<algorithm>
#include<vector>
using namespace std;
int main(){
  vector<int> v1(5);
  for(int k=0;k<5;k++){
    cout<<"v1["<<k<<"] ";
    cin>>v1[i];
  }
  sort(v1.begin(),v1.end());
  int i=0;
  for(int j=1;j<5;j++){
    if(v1[j]!=v1[i]){
      i++;
      v1[i]=v1[j];
    }
  }
  for(int i:v1){
    cout<<i<<" ";
  }
  return 0;
}
